package org.javaz;

import org.junit.*;

import java.util.*;
import java.util.function.*;
import java.util.stream.*;

public class InfiniteStreamTest {
/*
    @Test
    public void test1() {
        Stream<Integer> integers = Streams.iterate(1, i -> i + 1);
        Object[] result = integers.limit(25).toArray();
        System.out.println(Arrays.toString(result));
    }

    @Test
    public void test2() {
        Stream<Integer> integers = Streams.iterate(1, i -> i + 1);
        int[] result = integers.map(i -> i * i).limit(25).toArray();
        System.out.println(Arrays.toString(result));
    }
*/
}
